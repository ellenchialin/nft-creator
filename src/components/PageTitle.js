import { Heading } from '@chakra-ui/react';

const PageTitle = ({ title }) => {
  return (
    <Heading color="white" fontSize="41.88px">
      {title}
    </Heading>
  );
};

export default PageTitle;
